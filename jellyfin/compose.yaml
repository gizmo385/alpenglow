services:
  jellyfin:
    image: jellyfin/jellyfin
    container_name: jellyfin
    user: 110:114
    devices:
      - /dev/dri/renderD128:/dev/dri/renderD128
      - /dev/dri/card0:/dev/dri/card0
    group_add:
    - "992"
    volumes:
      - ./config:/config
      - /data/jellyfin/cache:/cache
      - type: bind
        source: /data/jellyfin/media
        target: /media
    restart: 'unless-stopped'
    environment:
      # Optional - alternative address used for autodiscovery
      - JELLYFIN_PublishedServerUrl=http://jellyfin.acbc.house
    networks:
      - caddy 
    labels:
      caddy_0: "*.acbc.house"
      caddy_0.@jellyfin.host: "jellyfin.acbc.house"
      caddy_0.reverse_proxy: "@jellyfin {{upstreams http 8096}}"
      caddy_1: "*.acbc.house:10443"
      caddy_1.@jellyfin.host: "jellyfin.acbc.house"
      caddy_1.reverse_proxy: "@jellyfin {{upstreams http 8096}}"

networks:
  caddy:
    external: true
